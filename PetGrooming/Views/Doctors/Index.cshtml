@using PetGrooming.Models
@model IEnumerable<Doctor>
@{
    ViewData["Title"] = "Doctors Management";
}

<div class="row mb-3">
    <div class="col-md-6">
        <h1><i class="fas fa-user-md"></i> Doctors Management</h1>
    </div>
    <div class="col-md-6 text-end">
        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus"></i> Add New Doctor
        </a>
    </div>
</div>


<div class="card">
    <div class="card-body">
        @if (Model.Any())
        {
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Name</th>
                            <th>Specialization</th>
                            <th>Contact</th>
                            <th>Assigned Pets</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var doctor in Model)
                        {
                            <tr class="@(doctor.IsActive ? "" : "table-secondary")">
                                <td>
                                    <strong>@doctor.Name</strong>
                                    <small class="text-muted d-block">Joined: @doctor.JoinDate.ToString("MMM yyyy")</small>
                                </td>
                                <td>@doctor.Specialization</td>
                                <td>
                                    @doctor.Phone
                                    @if (!string.IsNullOrEmpty(doctor.Email))
                                    {
                                        <small class="text-muted d-block">@doctor.Email</small>
                                    }
                                </td>
                                <td>
                                    <span class="badge bg-info">@doctor.Pets.Count() pets</span>
                                </td>
                                <td>
                                    @if (doctor.IsActive)
                                    {
                                        <span class="badge bg-success">Active</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary">Inactive</span>
                                    }
                                </td>
                                <td>
                                    <div class="btn-group" role="group">
                                        <a asp-action="Details" asp-route-id="@doctor.Id" class="btn btn-sm btn-info" title="Details">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                        <a asp-action="Edit" asp-route-id="@doctor.Id" class="btn btn-sm btn-warning" title="Edit">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <a asp-action="ToggleStatus" asp-route-id="@doctor.Id" class="btn btn-sm @(doctor.IsActive ? "btn-secondary" : "btn-success")" title="@(doctor.IsActive ? "Deactivate" : "Activate")">
                                            <i class="fas @(doctor.IsActive ? "fa-pause" : "fa-play")"></i>
                                        </a>
                                        @if (doctor.Pets.Count() == 0)
                                        {
                                            <a asp-action="Delete" asp-route-id="@doctor.Id" class="btn btn-sm btn-danger" title="Delete">
                                                <i class="fas fa-trash"></i>
                                            </a>
                                        }
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="text-center py-5">
                <i class="fas fa-user-md fa-3x text-muted mb-3"></i>
                <h5 class="text-muted">No doctors registered yet</h5>
                <p class="text-muted">Start by adding your first doctor to the system.</p>
                <a asp-action="Create" class="btn btn-primary">
                    <i class="fas fa-plus"></i> Add First Doctor
                </a>
            </div>
        }
    </div>
</div>